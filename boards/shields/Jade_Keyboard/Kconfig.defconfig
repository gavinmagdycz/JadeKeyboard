if SHIELD_JADE_KEYBOARD_LEFT

config ZMK_KEYBOARD_NAME
    default "Jade's Keyboard"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_JADE_KEYBOARD_LEFT || SHIELD_JADE_KEYBOARD_RIGHT

config ZMK_SPLIT
    default y

endif